# --- Database ---
DB_HOST=localhost\\SQLEXPRESS
DB_NAME=BoilerManagementDB
DB_USER=app_backend
DB_PASSWORD=Backend_P@ssw0rd_2026
DB_DRIVER=ODBC Driver 18 for SQL Server
DB_TRUST_CERT=yes

# --- JWT ---
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(48))"
JWT_SECRET_KEY=replace-me-with-a-64-char-random-string
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_DAYS=7

# --- App ---
APP_NAME=BoilerManagementAPI
APP_DEBUG=true
APP_HOST=0.0.0.0
APP_PORT=8000

# --- CORS ---
# Comma-separated list of allowed origins
CORS_ALLOWED_ORIGINS=http://localhost:5000,http://localhost:7000,http://localhost:5173
